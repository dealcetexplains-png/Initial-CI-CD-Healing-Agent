# Optional: for Gemini API support
# Install with: pip install -r requirements-optional.txt
google-generativeai>=0.3.0

# Python formatters (auto-fix LINTING/INDENTATION errors)
autopep8>=2.0.0
black>=23.0.0

# Python static analyzers (detect issues without AI)
pylint>=3.0.0
mypy>=1.0.0
bandit>=1.7.0

# Note: JavaScript/TypeScript tools (prettier, eslint) are installed via npm/npx on-demand
# Ruby tools (rubocop) require gem install rubocop
